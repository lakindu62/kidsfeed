import { CircleHelp, LogOut } from 'lucide-react';

export const DEFAULT_FOOTER_ACTIONS = Object.freeze([
  { key: 'support', label: 'Support', icon: CircleHelp },
  { key: 'logout', label: 'Log Out', icon: LogOut },
]);
